from enum import Enum
import os

from enum import Enum

class RepositoryType(Enum):
    """Represents the type of a repository.

    Attributes:
        REPOSITORY_CODE_C (str): Represents a C code repository.
    """

    REPOSITORY_CODE_C = "code-c"